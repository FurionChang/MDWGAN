1. 查找/建立一个生成模型与真值/数据集值相似性的指标（测试工具）  (ZH)
   statistical data of structure and moduli of random 6*6*6 dataset
2. training result of reference full random 6*6*6 dataset (H)

3. 测试收敛到1和2的原因是因为，上下界的原因，还是capture到数据集feature的原因 (H)
    In initialization,
        if data_set == 0, data_set = data_set+0.3
        if data_set == 1, data_set = data_set-0.3

4. Wasseratein_DCGAN vs W_DCGAN-p vs DCGAN (ZH)